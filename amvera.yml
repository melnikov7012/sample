version: null
meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"
build:
  requirementsPath: https://github.com/melnikov7012/sample/requirements.txt
  useCache: true
run:
  scriptName: ""
  persistenceMount: /data
  containerPort: 80
  command: uvicorn app.main:app --host 0.0.0.0 --port 8000
